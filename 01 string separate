#include <iostream>
#include <string>
using namespace std;
int main(){
    string a;
    getline(cin,a);
    if(!a.empty()){
        for(int i=0;i<a.size()-1;i++){
                cout<<a[i]<<" ";
        }
        cout<<a[a.size()-1];//可改成a.back()
    }
}
